2016-11-23

~/projects 라는 디렉토리 내에서 마지막수정시간이 A ~ B시간내에 존재하는 리스트 파일로 저장.

쉘스크립트로 실행 & awk 스크립트 활용


1. find 로 리스트 가져오고
2. 리스트 애들을 압축해서( tar.gz ), 이 압축파일은 ~/projects 내에 위치
3. 마지막으로 로그를 남김( 어떤파일들을 압축했는지 리스트랑, 시간 )


awk 

tip

| 으로 데이터 넘겨서 다음 exec라던지 ll -s를 실행해서 데이터를 정리할 수 있다.


/////////////////////////////////////////////////////////////////////////////////
scp 로 파일 옮기기
